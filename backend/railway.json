{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install && pip install -r scripts/requirements.txt && npm run build"
  },
  "deploy": {
    "startCommand": "npm run start-with-models",
    "restartPolicyType": "ON_FAILURE"
  }
}